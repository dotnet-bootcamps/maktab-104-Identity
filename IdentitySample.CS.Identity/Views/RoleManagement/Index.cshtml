@model List<AppRole>

<h1>مدیریت نقش ها</h1>

<table class="table table-striped w-100">
    <tr>
        <td>شناسه</td>
        <td>عنوان</td>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>
        </tr>
    }
</table>

